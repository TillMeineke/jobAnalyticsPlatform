name: 'job_analytics'
version: '1.0.0'
config-version: 2

profile: 'job_analytics'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  job_analytics:
    core:
      +materialized: table
      +schema: silver
    marts:
      +materialized: table
      +schema: gold